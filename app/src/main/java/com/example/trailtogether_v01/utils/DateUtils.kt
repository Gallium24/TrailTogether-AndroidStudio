package com.example.trailtogether_v01.utils

import java.text.SimpleDateFormat
import java.util.*

object DateUtils {
    fun formatRelativeTime(timestamp: String): String {
        // Simplifié pour le mock
        return "il y a 2h"
    }

    fun formatDate(date: String): String {
        return date
    }
}